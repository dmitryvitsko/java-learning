🎬 DateTime Comparator

📋 Описание

В приложении по прокату фильмов необходимо корректно сравнивать даты и время.

Для решения задачи реализован класс DateTimeComparator, который имплементирует интерфейс Comparator<DateTime>.

Компаратор последовательно сравнивает два объекта DateTime по всем полям:

Год

Месяц

День

Часы

Минуты

Секунды

🛠 Используемые технологии

Java

ООП

Generics

Comparator<T>

Integer.compare()

📁 Структура проекта

├── Main.java
├── DateTime.java
├── DateTimeComparator.java
└── RentedFilm.java

🔍 Логика сравнения

Метод compare() возвращает:

отрицательное число — если первая дата раньше второй;

0 — если даты полностью совпадают;

положительное число — если первая дата позже второй.

Сравнение происходит последовательно:

Год
↓
Месяц
↓
День
↓
Часы
↓
Минуты
↓
Секунды

💻 Пример использования

DateTime today = new DateTime(26, 11, 2021, 23, 58, 58);

DateTimeComparator comparator = new DateTimeComparator();

boolean shouldAlreadyBeReturned =
comparator.compare(today, film1.getTimeOfReturn()) > 0;

🧠 Что я изучил

В этой задаче я закрепил:

работу с интерфейсом Comparator<T>;

создание собственного компаратора;

реализацию метода compare();

последовательное сравнение полей объекта;

использование Integer.compare();

сравнение объектов пользовательского типа.

🚀 Запуск

Для запуска необходимо выполнить класс:

Main.java

Программа создаёт фильм с датой аренды и датой возврата, затем сравнивает текущую дату со сроком возврата и выводит результат в консоль.